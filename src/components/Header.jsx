import logo from "./../images.jpg";

const Header = () => {
    return (
        <header className="header">
            <img src={logo} alt="logo" />
        </header>
    )
}

export default Header;